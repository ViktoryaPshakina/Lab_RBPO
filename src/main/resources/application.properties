# ==============================================================
# ????????? ??????????? ? ???? ?????? (?? .env)
# ==============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/vet_clinic_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:RBPO1234}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# ????????? JPA / HIBERNATE
# ==============================================================
# 'update' ????????????? ??????? ??????????? ??????? (????????, user_sessions)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


# ???? ?? ??????, ????? Spring ???????? ???? ????? schema.sql.bak ? data.sql.bak:
spring.sql.init.mode=always

# ==============================================================
# ????????? JWT (????????? ? ???????, ??????? ? ???? JwtTokenProvider)
# ==============================================================
# ?? ????????? ??????? 'app.', ??? ??? ? ???? JwtTokenProvider ?? ???????????? @Value("${app.jwt.secret}")
app.jwt.secret=${jwt.secret:MySecretKeyForJwtAuthenticationVetClinicApplication2024}
app.jwt.access-expiration=${jwt.access-expiration:900000}
app.jwt.refresh-expiration=${jwt.refresh-expiration:604800000}

# ??? ??????? (????? ?????? ?????? ? ??????? ????? ????????)
logging.level.org.springframework.security=DEBUG